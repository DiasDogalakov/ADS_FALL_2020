#include <iostream>
#include <string>
#include <vector>
#include <set>
#include <map>
#include <cmath>
#include <queue>
 
using namespace std;
 
int main()
{
    queue<int> q1;
    queue<int> q2;
    int five = 5;
    int arr1[five];
    int arr2[five];
    for(int i = 0; i < five; i++)
    {
        cin >> arr1[i];
        q1.push(arr1[i]);
    }
    for(int i = 0; i < five; i++)
    {
        cin >> arr2[i];
        q2.push(arr2[i]);
    }
    while(!q1.empty() || !q2.empty())
    {
        if(q1[0] )
    }

}